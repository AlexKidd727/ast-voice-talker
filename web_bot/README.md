# Voice Talker Web Bot

Веб-бот для голосового общения , построенный на FastAPI с WebSocket поддержкой.

## Возможности

- 🎤 **Голосовое общение** - запись и распознавание речи в реальном времени
- 💬 **Текстовый чат** - общение через текстовые сообщения
- 📁 **Загрузка аудиофайлов** - поддержка различных форматов аудио
- 🔄 **WebSocket соединение** - общение в реальном времени
- 🤖 **LLM интеграция** - умные ответы через OpenAI API
- 💾 **Сохранение истории** - все разговоры сохраняются в базе данных

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r ../requirements.txt
```

### 2. Настройка конфигурации

Убедитесь, что в файле `../data/config.json` настроены необходимые параметры:

```json
{
  "llm": {
    "api_key": "YOUR_OPENAI_API_KEY",
    "model_name": "gpt-3.5-turbo"
  },
  "database": {
    "db_path": "data.db"
  }
}
```

### 3. Запуск веб-бота

```bash
cd web_bot
python app.py
```

Или с uvicorn:

```bash
uvicorn app:app --host 0.0.0.0 --port 8000 --reload
```

### 4. Открытие в браузере

Перейдите по адресу: http://localhost:8000

## Использование

### Голосовое общение

1. Нажмите кнопку "🎤 Голос"
2. Разрешите доступ к микрофону
3. Говорите в микрофон
4. Нажмите "⏹️ Стоп" для завершения записи
5. Получите ответ от бота

### Текстовый чат

1. Введите сообщение в поле ввода
2. Нажмите "Отправить" или Enter
3. Получите ответ от бота

### Загрузка аудиофайлов

1. Нажмите кнопку "📁 Файл"
2. Выберите аудиофайл (WAV, MP3, M4A, FLAC, OGG)
3. Файл будет обработан автоматически

### Команды

- `/help` - показать справку
- `/status` - статус системы
- `/clear` - очистить историю
- `/info` - информация о клиенте

## API Endpoints

### WebSocket

- `ws://localhost:8000/ws/{client_id}` - WebSocket соединение для реального времени

### HTTP API

- `GET /` - главная страница
- `POST /upload_audio` - загрузка аудиофайла
- `GET /api/status` - статус системы

## Структура проекта

```
web_bot/
├── app.py              # Основное приложение FastAPI
├── templates/
│   └── index.html      # HTML интерфейс
├── audio/              # Папка для сохранения аудиофайлов
└── README.md           # Документация
```

## Технические детали

### WebSocket протокол

Клиент подключается к WebSocket с уникальным ID:
```
ws://localhost:8000/ws/client_12345
```

### Типы сообщений

1. **Текстовые сообщения** - обычный текст
2. **Аудиоданные** - бинарные данные аудио
3. **Команды** - начинаются с `/`

### Обработка аудио

1. Получение аудиоданных через WebSocket или загрузку файла
2. Транскрипция через SpeechRecognition (Google API)
3. Анализ через LLM (OpenAI)
4. Отправка ответа клиенту
5. Сохранение в базу данных

## Безопасность

- CORS настроен для всех источников (в продакшене ограничить)
- Валидация типов файлов
- Ограничение размера аудиофайлов
- Логирование всех операций

## Мониторинг

Логи сохраняются в файл `web_bot.log` с подробной информацией о:
- Подключениях клиентов
- Обработке аудио
- Ошибках системы
- Статистике использования

## Развертывание

### Docker

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
EXPOSE 8000

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Nginx (рекомендуется для продакшена)

```nginx
location / {
    proxy_pass http://localhost:8000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
}
```

## Устранение неполадок

### Микрофон не работает

1. Проверьте разрешения браузера
2. Убедитесь, что используется HTTPS (для продакшена)
3. Проверьте настройки браузера

### LLM не отвечает

1. Проверьте API ключ OpenAI
2. Убедитесь в наличии интернет-соединения
3. Проверьте лимиты API

### База данных недоступна

1. Проверьте права доступа к файлу базы данных
2. Убедитесь, что директория `data/` существует
3. Проверьте логи на ошибки подключения
